#!/usr/bin/python3
def multiple_returns(sentence):
    s_length = len(sentence)
    if s_length == 0:
        first_char = none
    else:
        first_char = sentence[0]
    return((s_length, first_char))
